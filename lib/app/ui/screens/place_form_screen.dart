import 'package:flutter/material.dart';

class PlaceFormScreen extends StatelessWidget {
  static String routeName = '/place_form';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('place form'),
      ),
    );
  }
}
